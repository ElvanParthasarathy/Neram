package com.elvan.rmdneram.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Centralized App Colors
 * All shared colors are defined here to avoid duplication across screens.
 */
object AppColors {
    // === One UI Style Icon Colors ===
    val Blue = Color(0xFF2196F3)
    val Purple = Color(0xFF9C27B0)
    val Orange = Color(0xFFFF9800)
    val Green = Color(0xFF4CAF50)
    val Red = Color(0xFFF44336)
    val Teal = Color(0xFF009688)
    val Yellow = Color(0xFFFFC107)
    val Pink = Color(0xFFE91E63)
    
    // === Google Brand Color ===
    val GoogleBlue = Color(0xFF4285F4)
    
    // === Social/Brand Colors ===
    val GitHub = Color(0xFF10B981)
    val Instagram = Color(0xFFEC4899)
    val LinkedIn = Color(0xFF0A66C2)
    val YouTube = Color(0xFFEF4444)
    
    // === Surface Colors (AMOLED optimized) ===
    val DarkSurface = Color(0xFF1C1C1E)
    val DarkSurfaceVariant = Color(0xFF2C2C2C)
}
